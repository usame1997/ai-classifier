[tool.poetry]
name = "ai-classifier"
version = "0.1.0"
description = "AI Classifier Project"
authors = ["Your Name <your@email.com>"]

[tool.poetry.dependencies]
python = "^3.10.12"
# أضف هنا جميع المكتبات التي تستخدمها
flask = "^2.0.0"
torch = "^2.0.0"
transformers = "^4.30.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"